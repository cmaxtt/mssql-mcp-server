{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "mssql-schema": {
      "type": "local",
      "command": ["node", "dist/index.js"],
      "enabled": true,
      "environment": {},
      "description": "MCP server for SQL Server: schema inspection, DDL generation, view/procedure introspection, health checks, and optional read-only query execution"
    }
  }
}
